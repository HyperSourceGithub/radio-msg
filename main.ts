/**
 * 0 = off
 * 
 * 1 = on
 */
input.onButtonPressed(Button.AB, function () {
    if (cyclemode == 0) {
        cyclemode = 1
    } else {
        cyclemode = 0
    }
})
let cyclemode = 0
radio.setGroup(97)
cyclemode = 0
basic.forever(function () {
    if (true) {
    	
    } else {
    	
    }
})
